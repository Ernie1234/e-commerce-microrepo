# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @my-microrepo/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @my-microrepo/auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @my-microrepo/auth-service && \
          adduser --system -G @my-microrepo/auth-service @my-microrepo/auth-service

COPY apps/auth-service/dist @my-microrepo/auth-service/
RUN chown -R @my-microrepo/auth-service:@my-microrepo/auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @my-microrepo/auth-service --omit=dev -f install

CMD [ "node", "@my-microrepo/auth-service" ]
