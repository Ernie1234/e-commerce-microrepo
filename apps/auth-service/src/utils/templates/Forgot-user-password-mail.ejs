<!DOCTYPE html>
<html lang="en">
  <head>
    <title>E-commerce Password Reset Email</title>
    <meta http-equiv="Content-Type" content="text/html" charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      /* BASE STYLES */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        background-color: #fafafa;
        color: #222;
      }
      a {
        color: #000;
        text-decoration: none;
      }
      h1 {
        font-size: 24px;
        font-weight: 700;
        line-height: 1.25;
        margin-top: 0;
        margin-bottom: 15px;
        text-align: center;
      }
      p {
        margin-top: 0;
        margin-bottom: 24px;
      }
      table td {
        vertical-align: top;
      }

      /* LAYOUT STYLES */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
      }
      .email-header {
        background-color: #0070f3;
        padding: 24px;
        color: #fff;
      }
      .email-body {
        padding: 24px;
        background-color: #fff;
      }
      .email-footer {
        background-color: #f6f6f6;
        padding: 24px;
        text-align: center;
      }
      /* BUTTON STYLES (not used directly in this template, but kept for consistency if a link becomes a button) */
      .button {
        display: inline-block;
        padding: 12px 24px;
        background-color: #0070f3;
        color: #fff;
        font-size: 16px;
        font-weight: 700;
        text-align: center;
        text-decoration: none;
        border-radius: 4px;
      }
      .button:hover {
        background-color: #005ce6;
      }
      /* RESPONSIVE STYLES */
      @media screen and (max-width: 600px) {
        .email-wrapper {
          max-width: 100%;
        }
        .email-header {
          padding: 12px;
        }
        .email-body {
          padding: 12px;
        }
        .email-footer {
          padding: 12px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-header">
        <h1>Password Reset Request</h1>
      </div>
      <div class="email-body">
        <p>Dear <%= name %>,</p>
        <p>
          We received a request to reset your password for your E-commerce
          account.
        </p>
        <p>
          To proceed with your password reset, please use the following One-Time
          Password (OTP):
        </p>
        <h2><%= otp %></h2>
        <p>
          This OTP is valid for a limited time. Please enter it on the password
          reset page to set your new password.
        </p>
        <p>
          If you did not request a password reset, please ignore this email.
          Your password will remain unchanged.
        </p>
        <div class="email-footer">
          <p>Thank you for choosing E-commerce!</p>
          <p>Best regards,</p>
          <p>The E-commerce Team</p>
          <a href="mailto:support@ernie.com" id="href">Support@Ernie</a>
        </div>
      </div>
    </div>
  </body>
</html>
